import 'package:flutter/material.dart';

class AppColors {
  // Warna Kertas (Creamy/Yellowish)
  static const Color paperBackground = Color(0xFFFDF6E3);

  // Warna Tinta (Bukan hitam pekat, tapi dark grey/blue biar natural)
  static const Color ink = Color(0xFF2B2D42);

  // Warna Aksen (Pastel untuk highlight/stabilo)
  static const Color highlightGreen = Color(0xFFA7C957);
  static const Color highlightRed = Color(0xFFBC4749);

  // Garis Buku
  static const Color line = Color(0xFF94D2BD);
}
